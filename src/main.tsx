import React from "react";
import ReactDOM from "react-dom/client";
import Restaurants from "./components/App.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Restaurants />
  </React.StrictMode>
);
